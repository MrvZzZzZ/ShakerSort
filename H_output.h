#pragma once
#include <fstream>
#include <iostream>
#include <regex>

using namespace std;

void saveTheArray(int* array);
string Check_string();
bool Is_valid_filename(string& Filename);
bool Is_only_read(string Filename);
bool Is_file_exists(string Filename);

